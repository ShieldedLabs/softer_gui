# Pinned exactly: Rust has no LTS channel, so an exact stable version is the
# stability guarantee. Bump deliberately, never via `rustup update`.
[toolchain]
channel = "1.94.0"
targets = ["x86_64-unknown-linux-musl"]
profile = "minimal"
